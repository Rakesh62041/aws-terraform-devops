version: 3

projects:
  - name: terraform-dev
    dir: terraform
    workspace: dev
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "modules/**/*.tf"
        - "*.tfvars"
